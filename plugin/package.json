{
  "name": "@shipmate/plugin",
  "version": "0.3.0",
  "description": "ShipMate OpenClaw plugin — GitHub/GitLab/Jira tools with caching and rate limiting",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/**/*.test.ts"
  },
  "dependencies": {
    "@octokit/graphql": "^8.2.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "tsx": "^4.19.0",
    "@types/node": "^22.13.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
