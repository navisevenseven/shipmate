# ShipMate — Environment Variables
#
# Copy this file to .env and fill in your values.
# NEVER commit .env to git!

# ── Workspace ────────────────────────────────────────────────
# Absolute path to the target project on your host machine.
# This directory will be mounted into the container at /workspace.
SHIPMATE_WORKSPACE=/path/to/your/project

# Port for OpenClaw gateway (default: 3177)
SHIPMATE_PORT=3177

# ── GitHub (P0) ──────────────────────────────────────────────
# Fine-grained PAT scoped to a single repository.
# Permissions: Contents(R), PRs(RW), Issues(RW), Actions(R), Metadata(R)
# Create: github.com/settings/tokens → Fine-grained → Select repo
GITHUB_TOKEN=

# ── GitLab (P0) ─────────────────────────────────────────────
# Project Access Token with: read_api, read_repository, read_merge_request
# Create: GitLab → Settings → Access Tokens → Project Access Token
GITLAB_TOKEN=
GITLAB_HOST=https://gitlab.com

# ── Jira Cloud (P0) ─────────────────────────────────────────
# API token for Atlassian account
# Create: id.atlassian.com/manage-profile/security/api-tokens
JIRA_BASE_URL=
JIRA_API_TOKEN=
JIRA_USER_EMAIL=

# ── Sentry (P1) ─────────────────────────────────────────────
# Auth token with: project:read, event:read, issue:read
SENTRY_URL=
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=

# ── Grafana (P2) ────────────────────────────────────────────
# Service Account token with Viewer role
GRAFANA_URL=
GRAFANA_TOKEN=
