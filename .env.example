# ShipMate — Environment Variables
# Copy to .env and fill in your values.
# Run: ./setup/generate-scoped-env.sh to create .env.scoped for sandbox.

# ═══════════════════════════════════════════════
# PROJECT SCOPE (at least one required)
# Plugin tools are BLOCKED if scope is not set.
# install.sh auto-detects from git remote.
# ═══════════════════════════════════════════════

# GitHub repos (comma-separated, e.g. "owner/repo")
SHIPMATE_SCOPE_GITHUB_REPOS=

# GitLab projects (comma-separated, e.g. "group/project")
SHIPMATE_SCOPE_GITLAB_PROJECTS=

# Jira project keys (comma-separated, e.g. "SHIP,PROJ")
SHIPMATE_SCOPE_JIRA_PROJECTS=

# Jira board IDs (comma-separated, e.g. "42")
SHIPMATE_SCOPE_JIRA_BOARDS=

# Kubernetes namespaces (comma-separated, e.g. "production,staging")
SHIPMATE_SCOPE_K8S_NAMESPACES=

# ═══════════════════════════════════════════════
# TOKENS — always create scoped!
# ═══════════════════════════════════════════════

# GitHub: Fine-grained Personal Access Token
# Create: github.com/settings/tokens → Fine-grained → Select repo
# Repository access: Only select repositories → your project repo
# Permissions: Contents(R), PRs(RW), Issues(RW), Actions(R), Metadata(R)
# NEVER use classic PATs with broad 'repo' scope!
GITHUB_TOKEN=

# GitLab: Project Access Token (NOT Personal Access Token!)
# Create: GitLab → Settings → Access Tokens → Project Access Token
# Scopes: read_api, read_repository, read_merge_request
GITLAB_TOKEN=
GITLAB_HOST=

# Jira Cloud
# Create: id.atlassian.com/manage-profile/security/api-tokens
JIRA_BASE_URL=
JIRA_API_TOKEN=
JIRA_USER_EMAIL=

# ═══════════════════════════════════════════════
# OPTIONAL — monitoring
# ═══════════════════════════════════════════════

# Sentry (self-hosted)
SENTRY_URL=
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=

# Grafana
GRAFANA_URL=
GRAFANA_TOKEN=

# ═══════════════════════════════════════════════
# WORKSPACE
# ═══════════════════════════════════════════════
SHIPMATE_WORKSPACE=/path/to/your/project
